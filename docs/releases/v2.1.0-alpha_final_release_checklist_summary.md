# v2.1.0-alpha Final Release Checklist Summary

## Decision

**Current status: Release-ready.**

Recommendation: **enter tag / GitHub Release preparation next**, but **do not tag or publish until this summary is explicitly approved**.

## Final judgment

`v2.1.0-alpha` is not a routine small feature release. It is the version where Meridian Research Engine moves from **structure-level report gating** to **data-integrity-based release gating**.

Formal release eligibility now requires both:

- Data Integrity Hardlock = `PASS_FORMAL`
- Gatekeeper structure check = `FULL_PASS`

This is the key release threshold change for the alpha line.

## Release decision checklist

| Item | Status | Evidence |
|---|---|---|
| PR #4 Data Card + Earnings Basis | Done | already merged into `main` |
| PR #5 Capital Bridge + Hardlock Aggregator | Done | already merged into `main` |
| PR #6 Gatekeeper + Metadata Guard | Done | already merged into `main` |
| PR #7 Industry Hard Fields MVP | Done | already merged into `main` |
| PR #9 US_MANAGED_CARE generalization | Done | merged, commit `dae71df` |
| PR #8 release prep docs | Done | merged, commit `85bbb51` |
| Full test suite | Pass | `59 passed in 0.27s` |
| Focused hardlock regression suite | Pass | `42 passed in 0.02s` |
| Public repo boundary | Pass | keyword grep returned no hits |
| Public reports boundary | Pass | `reports/` contains SAMPLE-only files |
| `main` working tree | Clean | `git status --short` empty |
| `v2.1.0-alpha` tag | Not created | `git tag --list 'v2.1.0-alpha*'` returned empty |
| GitHub Release | Not created | no release step executed |

## What changed in this alpha line

1. Data Card registry and schema added.
2. Earnings Basis Hardlock added.
3. Capital Structure Bridge added.
4. Data Integrity Hardlock aggregator added.
5. Gatekeeper integrated with formal hardlock verdicts.
6. Valuation tools now downgrade unsupported bare-number outputs to `CALC_ONLY_NOT_FOR_CONCLUSION`.
7. Industry Hard Fields MVP added.
8. `US_MANAGED_CARE` generalized from UNH-specific field names to generic managed care segment fields before release.

## Boundary confirmation

The current public repository state remains within the intended boundary:

- no real-company smoke test materials in public repo
- no prior internal validation target materials in public repo
- no real-company sample reports in public repo
- no real price ranges
- no real valuation conclusions
- no real investment recommendations
- public reports remain SAMPLE-only

## Residual limitations

These are known and acceptable for `v2.1.0-alpha`:

- sidecar JSON MVP remains the current hardlock input format
- markdown body parsing is not yet implemented
- no automatic industry data fetching
- no full industry database

These are **not blockers** for the alpha release decision.

## Final recommendation

**Approve this summary, then proceed to:**

1. `git tag v2.1.0-alpha`
2. `gh release create v2.1.0-alpha`

## Explicit non-actions in this step

- no tag created
- no GitHub Release created
- no new PR created
- no code changed
